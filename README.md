# Boggle

> 2018 Boggle Solver

## Build Setup

``` bash
# Use node v11.0.0
nvm install node
nvm use 11.0.0

# install dependencies
yarn install

# serve with hot reload at localhost:8080
yarn run serve

# build for production with minification
yarn run build
```
